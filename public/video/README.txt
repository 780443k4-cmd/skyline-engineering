Optional: place a compressed hero background video here (MP4/WebM) if you want to
upgrade the hero from a static image to video. Not wired up by default - see
components/sections/Hero.tsx to swap the Image for a video with a poster image.
